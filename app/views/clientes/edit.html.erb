<%= render 'main/nav'%>
<%= link_to inline_svg_tag('GoBack.svg', size: '14x14'), :back, class: "voltar" %>

<main>
    <div class="main-content">
        <div class="main-top add-mec">
            <h1>Editar Cliente</h1>
        </div>
        <div class="main-info bg-w">
            <%= form_for @cliente , url: cliente_path(@cliente), method: :patch do |f| %>
                <div class="form-log">
                    <%= f.text_field :nome, placeholder: "Nome" %>
                    <%= f.email_field :email, placeholder: "Email" %>
                    <%= f.text_field :telefone, placeholder: "Telefone" %>
                    <%= f.text_field :endereco, placeholder: "Endereço" %>            
                    <%= f.submit "Editar", class: "entrarBtn" %>
                </div>
            <% end %>
        </div>
    </div>
</main>
